server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Beaconfire-Jul-2025/Config.git
          clone-on-start: true

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka/}


